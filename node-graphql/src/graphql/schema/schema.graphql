type Books {
  title: String
  author: Author
  price: Float
  categories: [String]
}

type Author {
  name: String
  age: Int
}

type Query {
  listBooks(title: String): [Books]
}

schema {
  query: Query
}